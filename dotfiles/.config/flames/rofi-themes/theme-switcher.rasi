configuration {
      modi: "drun";
      font: "DM Sans Medium 11.5";
      show-icons: true;
      icon-theme: "Papirus-dark";
      display-drun: "";
      drun-display-format: "{name}";
      sidebar-mode: false;
}

@theme "~/.config/rofi/colors.rasi"

* {
      bg: @surface;
      fg: @on-surface;
      accent: @primary;
      button: @surface-container;

      text-color: @fg;
}

window {
      location: center;
      ancor: center;
      border-radius: 7px;
      width: 80%;
      background-color: @bg;    /* main window bg */
      text-color: @fg;
}

prompt {
      background-color: transparent;   /* transparent */
      enabled: true;
      padding: 0.5% 10px 0% 10px;
      text-color: @on-surface;
}

entry {
    	placeholder: "Search";
	    background-color: transparent;   /* transparent */
    	placeholder-color: @outline;
    	expand: true;
    	padding: 0.15% 0% 0% 0%;
    	text-color: @fg;
}

inputbar {
	    children: [ prompt, entry ];
    	background-color: @button;      /* only inputbar has bg */
    	expand: false;
    	border-radius: 6px;
    	margin: 0%;
    	padding: 10px;
    	text-color: @inverse-surface;
}

listview {
    	columns: 1;
    	lines: 6;
    	cycle: false;
      spacing: 4em;
    	dynamic: true;
      location: center;
      ancor: center;
    	layout: horizontal;
      background-color: transparent;   /* transparent */
}

mainbox {
    	children: [ inputbar, listview ];
    	spacing: 2%;
    	padding: 2% 1% 2% 1%;
      background-color: transparent;   /* transparent */
}

element {
    	orientation: vertical;
    	padding: 2% 0% 2% 0%;
      background-color: transparent;   /* transparent */
}

element-icon {
    	size: 200px;
      horizontal: 10px;
      background-color: transparent;   /* transparent */
}

element-text {
    	expand: true;
    	horizontal-align: 0.5;
    	vertical-align: 0.5;
    	margin: 0.5% 0.5% -0.5% 0.5%;
    	text-color: @fg;
      background-color: transparent;   /* transparent */
}

element-text, element-icon {
	text-color: inherit;
	background-color: transparent;     /* transparent */
}

element-text selected {
    	background-color: @on-primary;   /* highlight */
    	border-radius: 6px;
      padding: 20px;
      margin: 0;
    	text-color: @fg;
}

